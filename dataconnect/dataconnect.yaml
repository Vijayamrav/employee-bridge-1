specVersion: "v1beta"
serviceId: "employee-portal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "employee-portal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
